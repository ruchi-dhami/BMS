module.exports = {
  testEnvironment: "node", // For Node.js projects
  testMatch: ["**/tests/**/*.test.js"], // Match test files in the `tests` folder
  verbose: true, // Display detailed results in the console
};
